import PanelMock from '@/components/sections/panel/panel-mock'

/** Statyczny podgląd zakładki Przegląd — używany na stronie głównej (sekcja CMS). */
export default function PanelOverviewMock() {
  return <PanelMock view="overview" className="max-w-3xl mx-auto" />
}
